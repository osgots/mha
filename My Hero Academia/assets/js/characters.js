/* =========================================
   MY HERO ACADEMIA UNIVERSE - CHARACTERS PAGE
========================================= */

'use strict';

class CharacterManager {
  constructor() {
    this.characterGrid = document.querySelector('.characters-grid');
    this.filterButtons = document.querySelectorAll('.filter-btn');
    this.searchInput = document.querySelector('#characterSearch');
    this.characterCards = document.querySelectorAll('....
